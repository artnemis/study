# Project: Artnemis

## Vision

Artnemis is an open-source AI-powered platform to help users prepare for exams.

It is NOT a generic learning platform.
It is an "AI exam preparation engine" with collaborative modules.

---

## Core Concepts

### Study Module

A Study Module is the main unit of the system.

It contains:

* study content
* AI configuration
* quiz generation rules
* collaboration settings

---

### Module Visibility

* public → visible and forkable by anyone
* private → accessible only by members

---

### Roles

* owner → full control
* editor → can modify content and config
* viewer → read-only access

---

### Study Plan

A structured schedule generated by AI based on:

* exam date
* topics
* user availability

---

### Quiz System

A quiz contains:

* multiple questions
* each question:

  * 4 options
  * 1 correct answer
  * explanation

---

### Adaptive Learning

The system must:

* track user mistakes
* prioritize weak topics
* adjust difficulty dynamically

---

### AI Provider (Pluggable)

Interface:

```ts
interface AIProvider {
  generateText(prompt: string): Promise<string>
  generateQuiz(input: QuizInput): Promise<Quiz>
  generatePlan(input: PlanInput): Promise<StudyPlan>
}
```

---

### AI Modes

* shared API key (default, limited)
* user API key (advanced mode)

---

## Collaboration

Users can:

* create modules
* invite collaborators
* fork public modules

---

## Tech Stack

Frontend:

* Next.js (App Router)
* TypeScript

Backend:

* Next.js API routes

Database:

* PostgreSQL

Auth:

* NextAuth.js

---

## Design Principles

* modular architecture
* pluggable AI providers
* strict typing
* simple and clear logic

---

## MVP Scope

* create module
* upload content
* generate plan
* generate quizzes
* basic collaboration (invite)

---

## Non Goals

* no social feed
* no marketplace
* no complex gamification (initially)

---

## Coding Rules

* no "any"
* small pure functions
* validate all AI outputs
* no business logic in UI

---

## Testing Strategy

We require high test coverage (>90%).

### Rules

* All core logic must be unit tested
* Validation must be fully tested
* Edge cases must be covered
* No untested business logic

### Test Types

* unit tests → core logic
* integration tests → API routes
* no heavy UI testing

### Mocking

* AIProvider must always be mocked
* no real API calls in tests

### Coverage Target

* global: 90%+
* core modules: 100%

### Important

Tests must verify behavior, not implementation.


---

## Important

AI output must never be trusted blindly.
Always validate and sanitize responses.
