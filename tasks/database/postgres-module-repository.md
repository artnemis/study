Implement PostgreSQL ModuleRepository adapter.

Requirements:

* persist modules, members and invites
* support getModuleById and listModules
* map DB rows to strict domain types
* keep token uniqueness at DB level