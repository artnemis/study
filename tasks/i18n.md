# Task: Multi-Language Support (i18n)

## Status: Done

## Description

Add internationalization with 5 languages: Italian, English, French, Spanish, German.

## Implementation

1. Created `lib/i18n/index.ts` — Dictionary interface, LOCALES, Locale type
2. Created dictionaries: `en.ts`, `it.ts`, `fr.ts`, `es.ts`, `de.ts`
3. Created `lib/i18n/context.tsx` — LocaleProvider, useLocale, useT hooks
4. Updated `app/layout.tsx` — wrapped app with LocaleProvider
5. Updated all pages to use `useT()` instead of hardcoded Italian strings:
   - Landing page (`app/page.tsx`)
   - Dashboard (`app/dashboard/page.tsx`)
   - Modules (`app/modules/page.tsx`)
   - Module detail (`app/modules/[moduleId]/page.tsx`)
   - Quiz (`app/quiz/quiz-content.tsx`)
   - Plans (`app/plans/plans-content.tsx`)
   - Navbar (`app/_components/navbar.tsx`)
6. Language selection in profile page persisted to cookie + user settings
